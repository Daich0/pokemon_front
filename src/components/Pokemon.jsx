/* import React from 'react'

function Pokemon({pokemon:{id,name,kind,image}}) {
  return (
    <div>
        <div className="mt-4">
          <p className="text-lg font-bold">Nombre del Pokémon: {pokemon.name}</p>
          <img src={pokemon.image} alt={pokemon.name} className="mt-2" />
        </div>
        <div
        key={id}
        className="flex p-3 bg-slate-50 rounded-sm shadow-md shadow-lime-600"
      ></div>
    s
    </div>
  )
}

export default Pokemon */